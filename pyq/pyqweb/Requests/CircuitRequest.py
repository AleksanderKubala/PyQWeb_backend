
class CircuitRequest(object):

    def __init__(self, size, state, layerCount, layers):
        self.size = size
        self.state = state
        self.layerCount = layerCount
        self.layers = layers
