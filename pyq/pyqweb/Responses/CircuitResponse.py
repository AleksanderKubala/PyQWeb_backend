
class CircuitResponse(object):

    def __init__(self, size, state, layer_count, layers):
        self.size = size
        self.state = state
        self.layer_count = layer_count
        self.layers = layers
